import java.util.Scanner;
class Main
{
    //amount A=p*(1+r)^n;
  public static void main(String[]args)
  {
      double am;
      int amount=principal+totalinterst;
      double pm=50000;
      double rate=0.02;
      double tremint=pm*rate;
      int totalterms=12;
      double totalinterst=tremint*totalterms;
      System.out.println("total amount is:"+amount);
     // System.out.println(tremint);
      /*for(int month=1;month<=12;month++)
      {
          am=pm*Math.pow(1+rate,month);
          System.out.println(month+" " +am);
      }*/
      
  }
}